import { ChooseIconDirective } from './choose-icon.directive';

describe('ChooseIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ChooseIconDirective(null);
    expect(directive).toBeTruthy();
  });
});
